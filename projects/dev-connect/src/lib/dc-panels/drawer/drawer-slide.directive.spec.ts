import { DrawerSlideDirective } from './drawer-slide.directive';

describe('DrawerSlideDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerSlideDirective();
    expect(directive).toBeTruthy();
  });
});
